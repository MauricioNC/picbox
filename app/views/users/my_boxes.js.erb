var boxes = document.querySelector("#user-content");

boxes.dataset.contentType = "user-images-downloaded"

boxes.removeChild(boxes.firstElementChild)
boxes.insertAdjacentHTML("beforeend", "<%= j( render :partial => 'boxes_card' ) %>");
